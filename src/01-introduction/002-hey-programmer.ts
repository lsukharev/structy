/**
 * hey programmer
 *
 * Welcome to Structy! Watch the approach video in the tab at the top of this
 * window to get started.
 *
 * Write a function, greet, that takes in a string argument, str, and returns
 * the string "hey str". No tricks here. Run the tests to check your work.
 */

export const greet = (str: string): string => {
  return 'hey ' + str;
};
